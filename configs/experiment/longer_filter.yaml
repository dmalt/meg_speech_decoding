# @package _global_
defaults:
    - override /dataset: meg
selected_channels: ${python_range:"range(306)"}
# lag_backward: 2000
model:
    regression:
        hidden_channels: 6
        filtering_size: 15
        envelope_size: 7
        use_lstm: False
