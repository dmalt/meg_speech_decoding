# @package _global_
defaults:
    - override /dataset: ecog
selected_channels: ${python_range:"range(6, 12)"}
target_features_cnt: 20
lag_backward: 1000
lag_forward: 0
model:
    learning_rate: 0.001
    regression:
        filtering_size: 25
        envelope_size: 15
